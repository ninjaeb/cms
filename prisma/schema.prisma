// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

enum Role {
  ADMIN
  EDITOR
}

model User {
  id           String   @id @default(cuid())
  email        String   @unique
  name         String
  passwordHash String
  role         Role     @default(EDITOR)
  createdAt    DateTime @default(now())
}

// One run of scanning `Setting.scanRootDir` (or an overridden path) for
// static HTML files and scoring each one for SEO/GEO quality.
model ScanRun {
  id          String     @id @default(cuid())
  rootDir     String
  startedAt   DateTime   @default(now())
  finishedAt  DateTime?
  pageCount   Int        @default(0)
  avgSeoScore Int?
  avgGeoScore Int?
  pages       PageScan[]
}

// One HTML file's extracted signals, checklist, and scores from a ScanRun.
model PageScan {
  id        String  @id @default(cuid())
  scanRunId String
  scanRun   ScanRun @relation(fields: [scanRunId], references: [id], onDelete: Cascade)

  filePath String
  urlPath  String
  title    String?

  seoScore Int
  geoScore Int

  // ChecklistItem[] as returned by computeChecklistFromPageSignals.
  checklist Json

  // Populated lazily when requested from the page detail view, not during
  // the scan itself, so scans stay fast and don't call the AI on every run.
  aiRecommendation String?

  scannedAt DateTime @default(now())

  @@index([scanRunId])
}

// Singleton site-wide configuration row.
model Setting {
  id                      Int     @id @default(1)
  scanRootDir             String  @default("")
  enableAiRecommendations Boolean @default(false)
}
